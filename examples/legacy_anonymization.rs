//! Descriptive alias for the legacy quickstart example.
//!
//! Retained separately so documentation can name the behavior being demonstrated
//! while existing references to `legacy_quickstart` remain valid.

include!("legacy_quickstart.rs");
